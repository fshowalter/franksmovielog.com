---
import React from "react";

import { Backdrop } from "~/components/Backdrop";
import { Collections } from "~/components/Collections/Collections";
import { getProps } from "~/components/Collections/getProps";
import { ListWithFiltersLayout } from "~/components/ListWithFiltersLayout";
import Layout from "~/layouts/Layout.astro";

const props = await getProps();

// Separate layout props from interactive props
const { backdropImageProps, deck, ...interactiveProps } = props;

// Create React elements in frontmatter
const backdropElement = React.createElement(Backdrop, {
  deck,
  imageProps: backdropImageProps,
  title: "Collections",
});
---

<Layout
  canonical={true}
  meta={{
    description:
      "A sortable index of movie collections reviewed on this site. Sort by name, review count, or title count.",
  }}
  openGraph={{ description: deck, type: "website" }}
  title="Collections"
>
  <ListWithFiltersLayout backdrop={backdropElement}>
    <Collections client:load {...interactiveProps} />
  </ListWithFiltersLayout>
</Layout>
