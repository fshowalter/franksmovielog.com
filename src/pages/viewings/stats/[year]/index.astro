---
import { allStatYears } from "~/api/stats";
import { getYearStatsProps } from "~/features/stats/getProps";
import { YearStats } from "~/features/stats/YearStats";
import Layout from "~/layouts/Layout.astro";

interface Props {
  year: string;
}

export async function getStaticPaths() {
  const statYears = await allStatYears();

  return statYears.map((year) => {
    return {
      params: {
        year: year,
      },
      props: {
        year: year,
      },
    };
  });
}

const { year } = Astro.props;

const props = await getYearStatsProps(year);
---

<Layout
  canonical={true}
  meta={{ description: props.metaDescription }}
  openGraph={{
    description: props.deck,
    type: "website",
  }}
  title={`${props.year} Stats`}
>
  <YearStats {...props} />
</Layout>
