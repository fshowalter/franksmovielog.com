---
import { getProps } from "src/components/Overrated/getProps";
import { Overrated } from "src/components/Overrated/Overrated";
import Layout from "src/layouts/Layout.astro";

const props = await getProps();
---

<Layout description={props.deck} title="Overrated Disappointments">
  <Overrated client:load {...props} />
</Layout>
