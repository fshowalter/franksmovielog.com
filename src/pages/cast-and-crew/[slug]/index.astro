---
import React from "react";

import { allCastAndCrew } from "~/api/cast-and-crew";
import { Backdrop, BreadcrumbLink } from "~/components/backdrop/Backdrop";
import { Layout as ComponentLayout } from "~/components/Layout/Layout";
import { CastAndCrewMemberTitlesStrictWrapper } from "~/features/cast-and-crew-member-titles/CastAndCrewMemberTitles";
import { getProps } from "~/features/cast-and-crew-member-titles/getProps";
import Layout from "~/layouts/Layout.astro";

interface Props {
  slug: string;
}

export async function getStaticPaths() {
  const { castAndCrew } = await allCastAndCrew();

  return castAndCrew.map((member) => {
    return {
      params: {
        slug: member.slug,
      },
      props: {
        slug: member.slug,
      },
    };
  });
}

const { slug } = Astro.props;
const props = await getProps(slug);

// Separate layout props from interactive props
const { backdropImageProps, deck, ...interactiveProps } = props;

const breadcrumb = React.createElement(BreadcrumbLink, {
  children: "Cast & Crew",
  href: "/cast-and-crew/",
});
---

<Layout
  canonical={true}
  meta={{ description: props.metaDescription }}
  openGraph={{
    description: deck,
    type: "website",
  }}
  title={`${props.name} movie reviews`}
>
  <ComponentLayout className="bg-subtle">
    <Backdrop
      breadcrumb={breadcrumb}
      deck={deck}
      imageProps={backdropImageProps}
      title={props.name}
    />
    <CastAndCrewMemberTitlesStrictWrapper
      client:load
      props={interactiveProps}
    />
  </ComponentLayout>
</Layout>
