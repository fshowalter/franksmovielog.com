export function ListItemReviewDate({ displayDate }: { displayDate: string }) {
  return (
    <div className={`font-sans text-xs leading-4 font-normal text-subtle`}>
      {displayDate}
    </div>
  );
}
